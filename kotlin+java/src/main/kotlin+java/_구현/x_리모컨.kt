package _구현

fun main() {
    var current = 100
    val next = readlnOrNull()?.toInt() ?: return
    readlnOrNull() ?: return
    val broken = readlnOrNull()?.split(" ")?.map { it.toInt() } ?: return
    var (minResult, maxResult) = listOf(0, 0)
    next.toString().forEach {
        
    }
}
